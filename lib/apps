#!/usr/bin/env bash

# Install native apps
run_apps() {
	# utils
	utils=(spotify transmission vlc whatsapp telegram slack)

	# dev
	dev=(cyberduck docker github google-chrome iterm2 postman visual-studio-code)
	
	for p in "${utils[@]} ${dev[@]}"; do
		brew cask install $p
	done
}
